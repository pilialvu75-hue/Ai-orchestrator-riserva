# templates/

Prompt templates and reusable AI instruction blueprints.

This directory contains structured prompt templates used by the orchestrator,
agents, and workflow engine to generate consistent AI interactions.

## Planned Contents

- `system_prompts/` — Base system prompts per AI provider
- `task_templates/` — Task-specific prompt scaffolds
- `workflow_templates/` — Reusable workflow definition templates (YAML/JSON)
