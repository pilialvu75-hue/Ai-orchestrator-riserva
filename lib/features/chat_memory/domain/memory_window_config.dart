// Compatibility re-export for callers that still import the historical
// feature-owned path. The canonical runtime contract now lives in core.
export 'package:ai_orchestrator/core/runtime/inference/memory_window_config.dart';
