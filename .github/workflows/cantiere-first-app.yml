name: Cantiere First App APK

on:
  pull_request:
    branches:
      - main
    paths:
      - 'tool/cantiere_first_app_smoke.dart'
      - 'tool/workshop_local_build_acceptance.dart'
      - 'test/app_factory/workshop/cantiere_first_app_smoke_test.dart'
      - 'lib/app_factory/**'
      - '.github/workflows/cantiere-first-app.yml'
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

concurrency:
  group: cantiere-first-app-${{ github.ref }}
  cancel-in-progress: true

jobs:
  first-app-android:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          submodules: false

      - name: Setup Java
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve AI Orchestrator dependencies
        run: flutter pub get

      - name: Analyze first-app acceptance smoke
        run: |
          dart analyze tool/cantiere_first_app_smoke.dart
          dart analyze tool/workshop_local_build_acceptance.dart
          flutter analyze test/app_factory/workshop/cantiere_first_app_smoke_test.dart

      - name: Create deterministic Android starter workspace
        id: workspace
        shell: bash
        run: |
          set -euo pipefail
          WORKSPACE="${RUNNER_TEMP}/cantiere-first-app"
          rm -rf "$WORKSPACE"
          flutter create \
            --platforms=android \
            --project-name=cantiere_first_app \
            --org=ai.orchestrator.generated \
            "$WORKSPACE"
          echo "path=$WORKSPACE" >> "$GITHUB_OUTPUT"

      - name: Run guarded Cantiere production lifecycle
        shell: bash
        env:
          CANTIERE_FIRST_APP_WORKSPACE: ${{ steps.workspace.outputs.path }}
          CANTIERE_FLUTTER_EXECUTABLE: ${{ env.FLUTTER_ROOT }}/bin/flutter
          CANTIERE_DART_EXECUTABLE: ${{ env.FLUTTER_ROOT }}/bin/dart
        run: |
          set -euo pipefail
          test -x "$CANTIERE_FLUTTER_EXECUTABLE"
          test -x "$CANTIERE_DART_EXECUTABLE"
          flutter test \
            test/app_factory/workshop/cantiere_first_app_smoke_test.dart \
            --reporter expanded \
            2>&1 | tee "${RUNNER_TEMP}/cantiere-first-app.log"

      - name: Validate prepared app through offline local Build Lab
        shell: bash
        env:
          CANTIERE_FLUTTER_EXECUTABLE: ${{ env.FLUTTER_ROOT }}/bin/flutter
          CANTIERE_DART_EXECUTABLE: ${{ env.FLUTTER_ROOT }}/bin/dart
        run: |
          set -euo pipefail
          test -x "$CANTIERE_FLUTTER_EXECUTABLE"
          test -x "$CANTIERE_DART_EXECUTABLE"
          "$CANTIERE_DART_EXECUTABLE" run             tool/workshop_local_build_acceptance.dart             "${{ steps.workspace.outputs.path }}"             2>&1 | tee -a "${RUNNER_TEMP}/cantiere-first-app.log"

      - name: Upload first-app diagnostics on failure
        if: ${{ failure() }}
        uses: actions/upload-artifact@v6
        with:
          name: cantiere-first-app-diagnostics
          path: ${{ runner.temp }}/cantiere-first-app.log
          if-no-files-found: warn
          retention-days: 7

      - name: Verify and package first APK
        shell: bash
        run: |
          set -euo pipefail
          SOURCE="${{ steps.workspace.outputs.path }}/build/app/outputs/flutter-apk/app-release.apk"
          test -s "$SOURCE"
          mkdir -p build/cantiere-first-app
          cp "$SOURCE" build/cantiere-first-app/cantiere-first-app.apk
          sha256sum build/cantiere-first-app/cantiere-first-app.apk \
            > build/cantiere-first-app/cantiere-first-app.apk.sha256
          cp "${RUNNER_TEMP}/cantiere-first-app.log" \
            build/cantiere-first-app/cantiere-first-app.log

      - name: Upload first app artifact
        uses: actions/upload-artifact@v6
        with:
          name: cantiere-first-app-android
          path: build/cantiere-first-app/
          if-no-files-found: error
          retention-days: 14
