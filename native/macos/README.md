# native/macos/

macOS-specific native code for AI Orchestrator.

This directory is the designated location for:
- macOS dylib helpers for on-device inference
- Metal/CoreML acceleration modules
- macOS-specific hardware acceleration helpers

> Currently a placeholder. macOS builds use the standard Flutter macOS runner.
