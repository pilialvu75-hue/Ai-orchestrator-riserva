# native/windows/

Windows-specific native code for AI Orchestrator.

This directory is the designated location for:
- Windows DLL helpers for on-device inference
- Win32 API integration modules
- Windows-specific hardware acceleration helpers (CUDA, DirectML)

> Currently a placeholder. Windows-specific Dart execution logic lives in
> `lib/native/runtime/windows/`.
